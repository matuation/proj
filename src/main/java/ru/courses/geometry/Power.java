package ru.courses.geometry;

public class Power {

    private Power Power(String x, String y) {
        return new Power(x,y);
    }

    static Power

    public double powerOfXY(String x, String y) {
        double xI = Integer.parseInt(x);
        double yI = Integer.parseInt(y);
        return Math.pow(xI, yI);
    }

}
